/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['flowbite.com', 'webknot.in','avatars.githubusercontent.com','cdn4.iconfinder.com','images.unsplash.com','example.com'],
    }
};

export default nextConfig;
